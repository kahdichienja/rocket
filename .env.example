# Copy to .env (git-ignored). Credentials come from DHA onboarding.
SHA_ENVIRONMENT=uat                 # uat | production
SHA_CLIENT_ID=
SHA_CLIENT_SECRET=
# Optional overrides (defaults derive from SHA_ENVIRONMENT):
# SHA_BASE_URL=https://ilm-dev.dha.go.ke/uat-middleware
# SHA_CONNECT_TIMEOUT=5
# SHA_READ_TIMEOUT=30
# SHA_UPLOAD_TIMEOUT=120
